/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['instagram.com', 'scontent.cdninstagram.com', 'graph.instagram.com'],
  },
  reactStrictMode: true,
}

module.exports = nextConfig
